apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plant-coach-helm.fullname" . }}-test-connection"
  labels:
    {{- include "plant-coach-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plant-coach-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
